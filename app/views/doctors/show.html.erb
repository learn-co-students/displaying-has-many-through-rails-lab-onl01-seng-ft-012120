<h1>Dr. <%= @doctor.name %></h1>

<h3>Appointments:</h3>

<ul>
<% @doctor.appointments.each do |appt| %>
    <li>
        <%= link_to appt.appointment_datetime.strftime("%B %e, %Y at %R"), appointment_path(appt)%> - 
        <%= link_to appt.patient.name, patient_path(appt.patient) %>
    </li>
<% end %>